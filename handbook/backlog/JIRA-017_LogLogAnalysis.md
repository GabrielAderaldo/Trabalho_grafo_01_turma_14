# JIRA-017: Dados para Escala Log-Log

## Descrição
Preparar os dados de frequência de graus de forma que possam ser facilmente plotados em escala Log-Log no Notebook para a análise de Escala Livre.

## Requisitos
- Exportar um arquivo `data/generated/log_log_data.csv` contendo `log(k)` e `log(P(k))`.
- Alternativamente, garantir que o CSV da JIRA-014 seja suficiente para o processamento no Python/Notebook.

## Definição de Pronto (DoD)
- Documentação no README sobre como importar esses dados no Notebook.
